import { StyleSheet } from "react-native";

const Colors = StyleSheet.create({
    primary: '#f90',
    light: {
        bgColor: '#fff',
        textColor: '#000'
    },
    dark: {
        bgColor: '#333',
        textColor: '#fff'
    }
});

export default Colors;