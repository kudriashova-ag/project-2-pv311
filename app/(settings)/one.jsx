import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const One = () => {
    return (
        <View style={{flex: 1}}>
            <Text>One</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default One;
