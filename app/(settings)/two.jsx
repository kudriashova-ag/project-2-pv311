import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Two = () => {
    return (
        <View style={{flex: 1}}>
            <Text>Two</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Two;
